extends layout 

block content 
   h2 Drivers
   p  there are #{drivers.length} drivers 

   ol 
      each driver in drivers 
         li 
            a(href=`/drivers/${driver.id}`) #{driver.name}

   